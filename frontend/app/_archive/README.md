# _archive

Retired components — not in active use but preserved for reference.
The `_` prefix prevents Next.js from generating routes for any files here.

- `DashboardPage.tsx` — old root dashboard (was `/dashboard`, now replaced by `/today`)
- `ChatWindow.tsx` — raw chat UI, replaced by PlanningPanel
- `ConfirmButtons.tsx` — replaced by PlanningPanel confirm flow
- `ScheduleCard.tsx` — replaced by in-place block materialisation
